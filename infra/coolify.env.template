# =============================================================================
# VEKLOM / LOCKERPHYCER — COOLIFY ENVIRONMENT VARIABLES
# =============================================================================
# Paste ALL of these into Coolify → Your App → Environment Variables
# Replace every ⚠️ ROTATE_FIRST value AFTER rotating at the service dashboard
# Everything else is already filled in correctly.
# =============================================================================

# ─────────────────────────────────────────────────────────────────────────────
# APPLICATION
# ─────────────────────────────────────────────────────────────────────────────
APP_NAME=Veklom Sovereign AI Hub
VERSION=1.0.0
ENVIRONMENT=production
DEBUG=false
LOG_LEVEL=INFO
LOG_FORMAT=json
LOG_FILE_PATH=/app/logs/lockerphycer.log

# ─────────────────────────────────────────────────────────────────────────────
# URLS
# ─────────────────────────────────────────────────────────────────────────────
FRONTEND_URL=https://app.veklom.com
API_URL=https://api.veklom.com

# ─────────────────────────────────────────────────────────────────────────────
# SECURITY KEYS — ⚠️ GENERATE FRESH VALUES (openssl rand -hex 32)
# ─────────────────────────────────────────────────────────────────────────────
SECRET_KEY=ROTATE_FIRST
AI_CITIZENSHIP_SECRET=ROTATE_FIRST
ENCRYPTION_KEY=ROTATE_FIRST
ADMIN_EMAIL=reprewindai@gmail.com

# ─────────────────────────────────────────────────────────────────────────────
# DATABASE — ⚠️ SET YOUR POSTGRES PASSWORD
# ─────────────────────────────────────────────────────────────────────────────
POSTGRES_DB=lockerphycer_production
POSTGRES_USER=lockerphycer_user
POSTGRES_PASSWORD=ROTATE_FIRST
DATABASE_URL=postgresql+asyncpg://lockerphycer_user:ROTATE_FIRST@postgres:5432/lockerphycer_production

# ─────────────────────────────────────────────────────────────────────────────
# REDIS / CELERY — ⚠️ SET YOUR REDIS PASSWORD
# ─────────────────────────────────────────────────────────────────────────────
REDIS_PASSWORD=ROTATE_FIRST
REDIS_URL=redis://:ROTATE_FIRST@redis:6379/0
CELERY_BROKER_URL=redis://:ROTATE_FIRST@redis:6379/0
CELERY_RESULT_BACKEND=redis://:ROTATE_FIRST@redis:6379/1

# ─────────────────────────────────────────────────────────────────────────────
# EMAIL — RESEND — ⚠️ ROTATE AT resend.com FIRST
# ─────────────────────────────────────────────────────────────────────────────
RESEND_API_KEY=ROTATE_FIRST
RESEND_SMTP_KEY=ROTATE_FIRST
EMAIL_FROM=Veklom <noreply@veklom.com>

# ─────────────────────────────────────────────────────────────────────────────
# SENTRY — ⚠️ ROTATE DSN AT sentry.io FIRST
# Login: pluggedfinds41@gmail.com
# Org: veklom-sovereign-ai-hub-p0
# SCIM URL: https://sentry.io/api/0/organizations/veklom-sovereign-ai-hub-p0/scim/v2
# ─────────────────────────────────────────────────────────────────────────────
SENTRY_DSN=ROTATE_FIRST
SENTRY_ENVIRONMENT=production
SENTRY_ORG=veklom-sovereign-ai-hub-p0
SENTRY_PROJECT=lockerphycer

# ─────────────────────────────────────────────────────────────────────────────
# GRAFANA CLOUD — CANADA EAST (prod-ca-east-0, aws ca-central-1)
# ⚠️ ROTATE TOKEN AT grafana.com → My Account → Access Policies
# One token covers ALL services below — needs scopes:
#   metrics:write  logs:write  traces:write  profiles:write  alerts:write
# ─────────────────────────────────────────────────────────────────────────────
GRAFANA_INSTANCE_ID=1652772
GRAFANA_API_TOKEN=ROTATE_FIRST
PROMETHEUS_API_TOKEN=ROTATE_FIRST

# Prometheus (Mimir) — username 3229768
PROMETHEUS_REMOTE_WRITE_URL=https://prometheus-prod-32-prod-ca-east-0.grafana.net/api/prom/push
PROMETHEUS_USERNAME=3229768
PROMETHEUS_PASSWORD=ROTATE_FIRST

# Loki — username 1610559
LOKI_URL=https://logs-prod-018.grafana.net
LOKI_USERNAME=1610559
LOKI_PASSWORD=ROTATE_FIRST

# Alertmanager — username 1612824
ALERTMANAGER_URL=https://alertmanager-prod-ca-east-0.grafana.net
ALERTMANAGER_USERNAME=1612824
ALERTMANAGER_PASSWORD=ROTATE_FIRST

# Pyroscope (Profiles) — username 1652772
PYROSCOPE_URL=https://profiles-prod-006.grafana.net
PYROSCOPE_USERNAME=1652772
PYROSCOPE_PASSWORD=ROTATE_FIRST

# Sigil AI Observability
SIGIL_ENDPOINT=https://sigil-prod-ca-east-0.grafana.net
SIGIL_INSTANCE_ID=1652772
SIGIL_API_TOKEN=ROTATE_FIRST

# OpenTelemetry — OTLP to Grafana Cloud
OTEL_SERVICE_NAME=veklom-sovereign-ai-hub
OTEL_EXPORTER_OTLP_ENDPOINT=https://otlp-gateway-prod-ca-east-0.grafana.net/otlp
OTEL_EXPORTER_OTLP_HEADERS=ROTATE_FIRST

# Grafana Alloy (Promtail replacement)
ALLOY_REMOTE_WRITE_URL=https://prometheus-prod-32-prod-ca-east-0.grafana.net/api/prom/push

# ─────────────────────────────────────────────────────────────────────────────
# AI PROVIDERS — ⚠️ FILL IN YOUR KEYS
# ─────────────────────────────────────────────────────────────────────────────
OPENAI_API_KEY=FILL_IN
HUGGINGFACE_API_KEY=FILL_IN
SERPAPI_KEY=ROTATE_FIRST

# ─────────────────────────────────────────────────────────────────────────────
# STRIPE BILLING — ⚠️ FILL IN FROM stripe.com DASHBOARD
# ─────────────────────────────────────────────────────────────────────────────
STRIPE_SECRET_KEY=FILL_IN
STRIPE_PUBLISHABLE_KEY=FILL_IN
STRIPE_WEBHOOK_SECRET=FILL_IN

# ─────────────────────────────────────────────────────────────────────────────
# SLACK — ALERTS
# ─────────────────────────────────────────────────────────────────────────────
SLACK_WORKSPACE=veklom.slack.com
SLACK_WEBHOOK_URL=FILL_IN

# ─────────────────────────────────────────────────────────────────────────────
# COOLIFY SERVER (5.78.135.11)
# ─────────────────────────────────────────────────────────────────────────────
SERVER_IP=5.78.135.11
COOLIFY_URL=https://coolify.yourdomain.com

# ─────────────────────────────────────────────────────────────────────────────
# PERFORMANCE SETTINGS (pre-filled, no changes needed)
# ─────────────────────────────────────────────────────────────────────────────
ENABLE_MFA=true
PASSWORD_MIN_LENGTH=8
MAX_FAILED_LOGIN_ATTEMPTS=10
ACCOUNT_LOCKOUT_DURATION_MINUTES=30
SESSION_TIMEOUT_MINUTES=120
MAX_CONCURRENT_REQUESTS=100
REQUEST_TIMEOUT_SECONDS=30
CACHE_TTL_SECONDS=3600
MAX_CONCURRENT_AI_REQUESTS=10
AI_REQUEST_TIMEOUT_SECONDS=60
MODEL_CACHE_DIR=/app/models
