interface:
  display_name: "Lockerphycer"
  short_description: "Repo-specific patterns and workflows for lockerphycer"
  default_prompt: "Use the lockerphycer repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true